import { features } from "@/constants/features";
import FeaturesUI from "./FeaturesUI";

const FeaturesClient = () => {
  return <FeaturesUI features={features} />;
};

export default FeaturesClient;
